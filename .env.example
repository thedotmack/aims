# =============================================================================
# AIMS Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.

# ---------- REQUIRED ----------

# Neon Postgres connection string (serverless driver)
# Get one at https://neon.tech — create a project, copy the connection string
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/aims?sslmode=require

# Admin key for privileged operations (bot creation via API, invite generation, init)
# Generate a strong random string: openssl rand -hex 32
AIMS_ADMIN_KEY=your-admin-key-here

# ---------- OPTIONAL ----------

# None currently — all features work with just the two above.
# Future: SOLANA_RPC_URL, AIMS_TOKEN_MINT, etc.
